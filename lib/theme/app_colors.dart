import 'package:flutter/material.dart';

class AppColors {
  const AppColors._();

  static const yellow = Color(0xFFFFCB05);
  static const navy = Color(0xFF0B1F3A);
  static const darkBlue = Color(0xFF0D47A1);

  static const lightBackground = Colors.white;
  static const lightSurface = Colors.white;
  static const lightText = navy;

  static const darkBackground = Color(0xFF101923);
  static const darkSurface = Color(0xFF162231);
  static const darkCardSurface = Color(0xFF1D2B3A);
  static const darkText = Colors.white;

  static const transparent = Colors.transparent;
  static const white = Colors.white;
  static const headerButtonBackground = Color(0x59000000);
  static const lightHeaderOverlay = Color(0x33000000);
  static const darkHeaderOverlay = Color(0x66000000);
}
